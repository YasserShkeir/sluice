{
  "name": "@sluice/app-loom",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Sluice Loom app: a self-contained plugin — adapter, parser, macOS Chrome cookie credential provider, and MCP tools that list the logged-in user's videos & notifications and fetch a video's transcript using the local Chrome session cookie.",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "node --import tsx --test src/*.test.ts"
  },
  "dependencies": {
    "@sluice/adapter-sdk": "workspace:*",
    "@sluice/core": "workspace:*",
    "better-sqlite3": "^11.7.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.12",
    "@types/node": "^20.10.0"
  }
}
