{
  "name": "@sluice/cartographer",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Sluice cartographer: turns captured traffic into an endpoint catalog and a per-app SQLite schema materialized from real responses; interaction-flow clustering and template learning.",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "node --import tsx --test src/*.test.ts"
  },
  "dependencies": {
    "@sluice/core": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^20.10.0"
  }
}
