{
  "name": "@sluice/interceptor",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "MITM + CDP capture engines, read-only replay and multi-step flow replay, safety rails.",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "node --import tsx --test src/*.test.ts"
  },
  "dependencies": {
    "@sluice/core": "workspace:*",
    "chrome-remote-interface": "^0.34.0",
    "mockttp": "^4.6.0"
  },
  "devDependencies": {
    "@types/chrome-remote-interface": "^0.34.0"
  }
}
